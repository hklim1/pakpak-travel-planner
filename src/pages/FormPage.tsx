// complete

import Body from '../components/Body'

export default function FormPage({ children }: {children: JSX.Element | JSX.Element[]}) {
  return (
    <Body>
        { children }
    </Body>
  )
}
